# Makefile.in generated by automake 1.10 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005, 2006  Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

# $Id: Makefile.am 1891 2005-02-07 21:58:18Z kuhlmann $ 

VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
subdir = doc
DIST_COMMON = $(srcdir)/Makefile.am $(srcdir)/Makefile.in
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/m4/binreloc.m4 \
	$(top_srcdir)/m4/iconv.m4 $(top_srcdir)/m4/lib-ld.m4 \
	$(top_srcdir)/m4/lib-link.m4 $(top_srcdir)/m4/lib-prefix.m4 \
	$(top_srcdir)/m4/libgcrypt.m4 $(top_srcdir)/m4/libgnutls.m4 \
	$(top_srcdir)/m4/libotr.m4 $(top_srcdir)/m4/pkg.m4 \
	$(top_srcdir)/acinclude.m4 $(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = $(top_builddir)/include/config.h
CONFIG_CLEAN_FILES =
SOURCES =
DIST_SOURCES =
man1dir = $(mandir)/man1
am__installdirs = "$(DESTDIR)$(man1dir)" "$(DESTDIR)$(man5dir)" \
	"$(DESTDIR)$(man7dir)" "$(DESTDIR)$(man1dedir)" \
	"$(DESTDIR)$(man1esdir)" "$(DESTDIR)$(man1frdir)" \
	"$(DESTDIR)$(man1itdir)" "$(DESTDIR)$(man1ptdir)" \
	"$(DESTDIR)$(man1rudir)" "$(DESTDIR)$(man1skdir)" \
	"$(DESTDIR)$(man1srdir)" "$(DESTDIR)$(man1ukdir)" \
	"$(DESTDIR)$(man5dedir)" "$(DESTDIR)$(man5esdir)" \
	"$(DESTDIR)$(man5frdir)" "$(DESTDIR)$(man5ptdir)" \
	"$(DESTDIR)$(man5rudir)" "$(DESTDIR)$(man5skdir)" \
	"$(DESTDIR)$(man5srdir)" "$(DESTDIR)$(man7dedir)" \
	"$(DESTDIR)$(man7esdir)" "$(DESTDIR)$(man7frdir)" \
	"$(DESTDIR)$(man7ptdir)" "$(DESTDIR)$(man7rudir)" \
	"$(DESTDIR)$(man7skdir)" "$(DESTDIR)$(man7srdir)" \
	"$(DESTDIR)$(man7ukdir)"
man5dir = $(mandir)/man5
man7dir = $(mandir)/man7
NROFF = nroff
MANS = $(man_MANS)
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = `echo $$p | sed -e 's|^.*/||'`;
man1deDATA_INSTALL = $(INSTALL_DATA)
man1esDATA_INSTALL = $(INSTALL_DATA)
man1frDATA_INSTALL = $(INSTALL_DATA)
man1itDATA_INSTALL = $(INSTALL_DATA)
man1ptDATA_INSTALL = $(INSTALL_DATA)
man1ruDATA_INSTALL = $(INSTALL_DATA)
man1skDATA_INSTALL = $(INSTALL_DATA)
man1srDATA_INSTALL = $(INSTALL_DATA)
man1ukDATA_INSTALL = $(INSTALL_DATA)
man5deDATA_INSTALL = $(INSTALL_DATA)
man5esDATA_INSTALL = $(INSTALL_DATA)
man5frDATA_INSTALL = $(INSTALL_DATA)
man5ptDATA_INSTALL = $(INSTALL_DATA)
man5ruDATA_INSTALL = $(INSTALL_DATA)
man5skDATA_INSTALL = $(INSTALL_DATA)
man5srDATA_INSTALL = $(INSTALL_DATA)
man7deDATA_INSTALL = $(INSTALL_DATA)
man7esDATA_INSTALL = $(INSTALL_DATA)
man7frDATA_INSTALL = $(INSTALL_DATA)
man7ptDATA_INSTALL = $(INSTALL_DATA)
man7ruDATA_INSTALL = $(INSTALL_DATA)
man7skDATA_INSTALL = $(INSTALL_DATA)
man7srDATA_INSTALL = $(INSTALL_DATA)
man7ukDATA_INSTALL = $(INSTALL_DATA)
DATA = $(man1de_DATA) $(man1es_DATA) $(man1fr_DATA) $(man1it_DATA) \
	$(man1pt_DATA) $(man1ru_DATA) $(man1sk_DATA) $(man1sr_DATA) \
	$(man1uk_DATA) $(man5de_DATA) $(man5es_DATA) $(man5fr_DATA) \
	$(man5pt_DATA) $(man5ru_DATA) $(man5sk_DATA) $(man5sr_DATA) \
	$(man7de_DATA) $(man7es_DATA) $(man7fr_DATA) $(man7pt_DATA) \
	$(man7ru_DATA) $(man7sk_DATA) $(man7sr_DATA) $(man7uk_DATA)
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = @ACLOCAL@
AMTAR = @AMTAR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
BINRELOC_CFLAGS = @BINRELOC_CFLAGS@
BINRELOC_LIBS = @BINRELOC_LIBS@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
CXX = @CXX@
CXXCPP = @CXXCPP@
CXXDEPMODE = @CXXDEPMODE@
CXXFLAGS = @CXXFLAGS@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
EXEEXT = @EXEEXT@
GREP = @GREP@
INSTALL = @INSTALL@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
LDFLAGS = @LDFLAGS@
LIBGCRYPT_CFLAGS = @LIBGCRYPT_CFLAGS@
LIBGCRYPT_CONFIG = @LIBGCRYPT_CONFIG@
LIBGCRYPT_LIBS = @LIBGCRYPT_LIBS@
LIBGNUTLS_CFLAGS = @LIBGNUTLS_CFLAGS@
LIBGNUTLS_CONFIG = @LIBGNUTLS_CONFIG@
LIBGNUTLS_LIBS = @LIBGNUTLS_LIBS@
LIBICONV = @LIBICONV@
LIBMSN = @LIBMSN@
LIBOBJS = @LIBOBJS@
LIBOTR_CFLAGS = @LIBOTR_CFLAGS@
LIBOTR_LIBS = @LIBOTR_LIBS@
LIBS = @LIBS@
LIBTCL_LIBS = @LIBTCL_LIBS@
LIBXMPP = @LIBXMPP@
LN_S = @LN_S@
LTLIBICONV = @LTLIBICONV@
LTLIBOBJS = @LTLIBOBJS@
L_LIBMSN = @L_LIBMSN@
L_LIBXMPP = @L_LIBXMPP@
MAINT = @MAINT@
MAKEINFO = @MAKEINFO@
MKDIR_P = @MKDIR_P@
OBJEXT = @OBJEXT@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
PKG_CONFIG = @PKG_CONFIG@
RANLIB = @RANLIB@
RELAYTOOL = @RELAYTOOL@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
STRIP = @STRIP@
VERSION = @VERSION@
XMPP_CFLAGS = @XMPP_CFLAGS@
XMPP_LIBS = @XMPP_LIBS@
abs_builddir = @abs_builddir@
abs_srcdir = @abs_srcdir@
abs_top_builddir = @abs_top_builddir@
abs_top_srcdir = @abs_top_srcdir@
ac_ct_CC = @ac_ct_CC@
ac_ct_CXX = @ac_ct_CXX@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
am__tar = @am__tar@
am__untar = @am__untar@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
builddir = @builddir@
datadir = @datadir@
datarootdir = @datarootdir@
docdir = @docdir@
dvidir = @dvidir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
htmldir = @htmldir@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
pdfdir = @pdfdir@
prefix = @prefix@
program_transform_name = @program_transform_name@
psdir = @psdir@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
srcdir = @srcdir@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
top_builddir = @top_builddir@
top_srcdir = @top_srcdir@
man1dedir = @mandir@/de/man1
man1de_DATA = de/climm.1
man5dedir = @mandir@/de/man5
man5de_DATA = de/climmrc.5
man7dedir = @mandir@/de/man7
man7de_DATA = de/climmcmds.7
man1skdir = @mandir@/sk/man1
man1sk_DATA = sk/climm.1
man5skdir = @mandir@/sk/man5
man5sk_DATA = sk/climmrc.5
man7skdir = @mandir@/sk/man7
man7sk_DATA = sk/climmcmds.7
man1frdir = @mandir@/fr/man1
man1fr_DATA = fr/climm.1
man5frdir = @mandir@/fr/man5
man5fr_DATA = fr/climmrc.5
man7frdir = @mandir@/fr/man7
man7fr_DATA = fr/climmcmds.7
man1esdir = @mandir@/es/man1
man1es_DATA = es/climm.1
man5esdir = @mandir@/es/man5
man5es_DATA = es/climmrc.5
man7esdir = @mandir@/es/man7
man7es_DATA = es/climmcmds.7
man1rudir = @mandir@/ru/man1
man1ru_DATA = ru/climm.1
man5rudir = @mandir@/ru/man5
man5ru_DATA = ru/climmrc.5
man7rudir = @mandir@/ru/man7
man7ru_DATA = ru/climmcmds.7
man1ptdir = @mandir@/pt_BR/man1
man1pt_DATA = pt_BR/climm.1
man5ptdir = @mandir@/pt_BR/man5
man5pt_DATA = pt_BR/climmrc.5
man7ptdir = @mandir@/pt_BR/man7
man7pt_DATA = pt_BR/climmcmds.7
man1ukdir = @mandir@/uk/man1
man1uk_DATA = uk/climm.1
#man5ukdir   = @mandir@/uk/man5
#man5uk_DATA = uk/climmrc.5
man7ukdir = @mandir@/uk/man7
man7uk_DATA = uk/climmcmds.7
man1srdir = @mandir@/sr/man1
man1sr_DATA = sr/climm.1
man5srdir = @mandir@/sr/man5
man5sr_DATA = sr/climmrc.5
man7srdir = @mandir@/sr/man7
man7sr_DATA = sr/climmcmds.7
man1itdir = @mandir@/it/man1
man1it_DATA = it/climm.1
#man5itdir   = @mandir@/it/man5
#man5it_DATA = it/climmrc.5
#man7itdir   = @mandir@/it/man7
#man7it_DATA = it/climmcmds.7
man_MANS = climm.1    climmcmds.7    climmrc.5
doc_DIST = README README.SOCKS5 commands.txt icq091.txt html/*.html html/*.gif
all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in: @MAINTAINER_MODE_TRUE@ $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh \
		&& exit 0; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --gnu  doc/Makefile'; \
	cd $(top_srcdir) && \
	  $(AUTOMAKE) --gnu  doc/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure: @MAINTAINER_MODE_TRUE@ $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4): @MAINTAINER_MODE_TRUE@ $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
install-man1: $(man1_MANS) $(man_MANS)
	@$(NORMAL_INSTALL)
	test -z "$(man1dir)" || $(MKDIR_P) "$(DESTDIR)$(man1dir)"
	@list='$(man1_MANS) $(dist_man1_MANS) $(nodist_man1_MANS)'; \
	l2='$(man_MANS) $(dist_man_MANS) $(nodist_man_MANS)'; \
	for i in $$l2; do \
	  case "$$i" in \
	    *.1*) list="$$list $$i" ;; \
	  esac; \
	done; \
	for i in $$list; do \
	  if test -f $(srcdir)/$$i; then file=$(srcdir)/$$i; \
	  else file=$$i; fi; \
	  ext=`echo $$i | sed -e 's/^.*\\.//'`; \
	  case "$$ext" in \
	    1*) ;; \
	    *) ext='1' ;; \
	  esac; \
	  inst=`echo $$i | sed -e 's/\\.[0-9a-z]*$$//'`; \
	  inst=`echo $$inst | sed -e 's/^.*\///'`; \
	  inst=`echo $$inst | sed '$(transform)'`.$$ext; \
	  echo " $(INSTALL_DATA) '$$file' '$(DESTDIR)$(man1dir)/$$inst'"; \
	  $(INSTALL_DATA) "$$file" "$(DESTDIR)$(man1dir)/$$inst"; \
	done
uninstall-man1:
	@$(NORMAL_UNINSTALL)
	@list='$(man1_MANS) $(dist_man1_MANS) $(nodist_man1_MANS)'; \
	l2='$(man_MANS) $(dist_man_MANS) $(nodist_man_MANS)'; \
	for i in $$l2; do \
	  case "$$i" in \
	    *.1*) list="$$list $$i" ;; \
	  esac; \
	done; \
	for i in $$list; do \
	  ext=`echo $$i | sed -e 's/^.*\\.//'`; \
	  case "$$ext" in \
	    1*) ;; \
	    *) ext='1' ;; \
	  esac; \
	  inst=`echo $$i | sed -e 's/\\.[0-9a-z]*$$//'`; \
	  inst=`echo $$inst | sed -e 's/^.*\///'`; \
	  inst=`echo $$inst | sed '$(transform)'`.$$ext; \
	  echo " rm -f '$(DESTDIR)$(man1dir)/$$inst'"; \
	  rm -f "$(DESTDIR)$(man1dir)/$$inst"; \
	done
install-man5: $(man5_MANS) $(man_MANS)
	@$(NORMAL_INSTALL)
	test -z "$(man5dir)" || $(MKDIR_P) "$(DESTDIR)$(man5dir)"
	@list='$(man5_MANS) $(dist_man5_MANS) $(nodist_man5_MANS)'; \
	l2='$(man_MANS) $(dist_man_MANS) $(nodist_man_MANS)'; \
	for i in $$l2; do \
	  case "$$i" in \
	    *.5*) list="$$list $$i" ;; \
	  esac; \
	done; \
	for i in $$list; do \
	  if test -f $(srcdir)/$$i; then file=$(srcdir)/$$i; \
	  else file=$$i; fi; \
	  ext=`echo $$i | sed -e 's/^.*\\.//'`; \
	  case "$$ext" in \
	    5*) ;; \
	    *) ext='5' ;; \
	  esac; \
	  inst=`echo $$i | sed -e 's/\\.[0-9a-z]*$$//'`; \
	  inst=`echo $$inst | sed -e 's/^.*\///'`; \
	  inst=`echo $$inst | sed '$(transform)'`.$$ext; \
	  echo " $(INSTALL_DATA) '$$file' '$(DESTDIR)$(man5dir)/$$inst'"; \
	  $(INSTALL_DATA) "$$file" "$(DESTDIR)$(man5dir)/$$inst"; \
	done
uninstall-man5:
	@$(NORMAL_UNINSTALL)
	@list='$(man5_MANS) $(dist_man5_MANS) $(nodist_man5_MANS)'; \
	l2='$(man_MANS) $(dist_man_MANS) $(nodist_man_MANS)'; \
	for i in $$l2; do \
	  case "$$i" in \
	    *.5*) list="$$list $$i" ;; \
	  esac; \
	done; \
	for i in $$list; do \
	  ext=`echo $$i | sed -e 's/^.*\\.//'`; \
	  case "$$ext" in \
	    5*) ;; \
	    *) ext='5' ;; \
	  esac; \
	  inst=`echo $$i | sed -e 's/\\.[0-9a-z]*$$//'`; \
	  inst=`echo $$inst | sed -e 's/^.*\///'`; \
	  inst=`echo $$inst | sed '$(transform)'`.$$ext; \
	  echo " rm -f '$(DESTDIR)$(man5dir)/$$inst'"; \
	  rm -f "$(DESTDIR)$(man5dir)/$$inst"; \
	done
install-man7: $(man7_MANS) $(man_MANS)
	@$(NORMAL_INSTALL)
	test -z "$(man7dir)" || $(MKDIR_P) "$(DESTDIR)$(man7dir)"
	@list='$(man7_MANS) $(dist_man7_MANS) $(nodist_man7_MANS)'; \
	l2='$(man_MANS) $(dist_man_MANS) $(nodist_man_MANS)'; \
	for i in $$l2; do \
	  case "$$i" in \
	    *.7*) list="$$list $$i" ;; \
	  esac; \
	done; \
	for i in $$list; do \
	  if test -f $(srcdir)/$$i; then file=$(srcdir)/$$i; \
	  else file=$$i; fi; \
	  ext=`echo $$i | sed -e 's/^.*\\.//'`; \
	  case "$$ext" in \
	    7*) ;; \
	    *) ext='7' ;; \
	  esac; \
	  inst=`echo $$i | sed -e 's/\\.[0-9a-z]*$$//'`; \
	  inst=`echo $$inst | sed -e 's/^.*\///'`; \
	  inst=`echo $$inst | sed '$(transform)'`.$$ext; \
	  echo " $(INSTALL_DATA) '$$file' '$(DESTDIR)$(man7dir)/$$inst'"; \
	  $(INSTALL_DATA) "$$file" "$(DESTDIR)$(man7dir)/$$inst"; \
	done
uninstall-man7:
	@$(NORMAL_UNINSTALL)
	@list='$(man7_MANS) $(dist_man7_MANS) $(nodist_man7_MANS)'; \
	l2='$(man_MANS) $(dist_man_MANS) $(nodist_man_MANS)'; \
	for i in $$l2; do \
	  case "$$i" in \
	    *.7*) list="$$list $$i" ;; \
	  esac; \
	done; \
	for i in $$list; do \
	  ext=`echo $$i | sed -e 's/^.*\\.//'`; \
	  case "$$ext" in \
	    7*) ;; \
	    *) ext='7' ;; \
	  esac; \
	  inst=`echo $$i | sed -e 's/\\.[0-9a-z]*$$//'`; \
	  inst=`echo $$inst | sed -e 's/^.*\///'`; \
	  inst=`echo $$inst | sed '$(transform)'`.$$ext; \
	  echo " rm -f '$(DESTDIR)$(man7dir)/$$inst'"; \
	  rm -f "$(DESTDIR)$(man7dir)/$$inst"; \
	done
install-man1deDATA: $(man1de_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man1dedir)" || $(MKDIR_P) "$(DESTDIR)$(man1dedir)"
	@list='$(man1de_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man1deDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man1dedir)/$$f'"; \
	  $(man1deDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man1dedir)/$$f"; \
	done

uninstall-man1deDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man1de_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man1dedir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man1dedir)/$$f"; \
	done
install-man1esDATA: $(man1es_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man1esdir)" || $(MKDIR_P) "$(DESTDIR)$(man1esdir)"
	@list='$(man1es_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man1esDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man1esdir)/$$f'"; \
	  $(man1esDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man1esdir)/$$f"; \
	done

uninstall-man1esDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man1es_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man1esdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man1esdir)/$$f"; \
	done
install-man1frDATA: $(man1fr_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man1frdir)" || $(MKDIR_P) "$(DESTDIR)$(man1frdir)"
	@list='$(man1fr_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man1frDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man1frdir)/$$f'"; \
	  $(man1frDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man1frdir)/$$f"; \
	done

uninstall-man1frDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man1fr_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man1frdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man1frdir)/$$f"; \
	done
install-man1itDATA: $(man1it_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man1itdir)" || $(MKDIR_P) "$(DESTDIR)$(man1itdir)"
	@list='$(man1it_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man1itDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man1itdir)/$$f'"; \
	  $(man1itDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man1itdir)/$$f"; \
	done

uninstall-man1itDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man1it_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man1itdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man1itdir)/$$f"; \
	done
install-man1ptDATA: $(man1pt_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man1ptdir)" || $(MKDIR_P) "$(DESTDIR)$(man1ptdir)"
	@list='$(man1pt_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man1ptDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man1ptdir)/$$f'"; \
	  $(man1ptDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man1ptdir)/$$f"; \
	done

uninstall-man1ptDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man1pt_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man1ptdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man1ptdir)/$$f"; \
	done
install-man1ruDATA: $(man1ru_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man1rudir)" || $(MKDIR_P) "$(DESTDIR)$(man1rudir)"
	@list='$(man1ru_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man1ruDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man1rudir)/$$f'"; \
	  $(man1ruDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man1rudir)/$$f"; \
	done

uninstall-man1ruDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man1ru_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man1rudir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man1rudir)/$$f"; \
	done
install-man1skDATA: $(man1sk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man1skdir)" || $(MKDIR_P) "$(DESTDIR)$(man1skdir)"
	@list='$(man1sk_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man1skDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man1skdir)/$$f'"; \
	  $(man1skDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man1skdir)/$$f"; \
	done

uninstall-man1skDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man1sk_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man1skdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man1skdir)/$$f"; \
	done
install-man1srDATA: $(man1sr_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man1srdir)" || $(MKDIR_P) "$(DESTDIR)$(man1srdir)"
	@list='$(man1sr_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man1srDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man1srdir)/$$f'"; \
	  $(man1srDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man1srdir)/$$f"; \
	done

uninstall-man1srDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man1sr_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man1srdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man1srdir)/$$f"; \
	done
install-man1ukDATA: $(man1uk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man1ukdir)" || $(MKDIR_P) "$(DESTDIR)$(man1ukdir)"
	@list='$(man1uk_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man1ukDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man1ukdir)/$$f'"; \
	  $(man1ukDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man1ukdir)/$$f"; \
	done

uninstall-man1ukDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man1uk_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man1ukdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man1ukdir)/$$f"; \
	done
install-man5deDATA: $(man5de_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man5dedir)" || $(MKDIR_P) "$(DESTDIR)$(man5dedir)"
	@list='$(man5de_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man5deDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man5dedir)/$$f'"; \
	  $(man5deDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man5dedir)/$$f"; \
	done

uninstall-man5deDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man5de_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man5dedir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man5dedir)/$$f"; \
	done
install-man5esDATA: $(man5es_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man5esdir)" || $(MKDIR_P) "$(DESTDIR)$(man5esdir)"
	@list='$(man5es_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man5esDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man5esdir)/$$f'"; \
	  $(man5esDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man5esdir)/$$f"; \
	done

uninstall-man5esDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man5es_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man5esdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man5esdir)/$$f"; \
	done
install-man5frDATA: $(man5fr_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man5frdir)" || $(MKDIR_P) "$(DESTDIR)$(man5frdir)"
	@list='$(man5fr_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man5frDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man5frdir)/$$f'"; \
	  $(man5frDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man5frdir)/$$f"; \
	done

uninstall-man5frDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man5fr_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man5frdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man5frdir)/$$f"; \
	done
install-man5ptDATA: $(man5pt_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man5ptdir)" || $(MKDIR_P) "$(DESTDIR)$(man5ptdir)"
	@list='$(man5pt_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man5ptDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man5ptdir)/$$f'"; \
	  $(man5ptDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man5ptdir)/$$f"; \
	done

uninstall-man5ptDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man5pt_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man5ptdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man5ptdir)/$$f"; \
	done
install-man5ruDATA: $(man5ru_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man5rudir)" || $(MKDIR_P) "$(DESTDIR)$(man5rudir)"
	@list='$(man5ru_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man5ruDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man5rudir)/$$f'"; \
	  $(man5ruDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man5rudir)/$$f"; \
	done

uninstall-man5ruDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man5ru_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man5rudir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man5rudir)/$$f"; \
	done
install-man5skDATA: $(man5sk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man5skdir)" || $(MKDIR_P) "$(DESTDIR)$(man5skdir)"
	@list='$(man5sk_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man5skDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man5skdir)/$$f'"; \
	  $(man5skDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man5skdir)/$$f"; \
	done

uninstall-man5skDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man5sk_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man5skdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man5skdir)/$$f"; \
	done
install-man5srDATA: $(man5sr_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man5srdir)" || $(MKDIR_P) "$(DESTDIR)$(man5srdir)"
	@list='$(man5sr_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man5srDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man5srdir)/$$f'"; \
	  $(man5srDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man5srdir)/$$f"; \
	done

uninstall-man5srDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man5sr_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man5srdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man5srdir)/$$f"; \
	done
install-man7deDATA: $(man7de_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man7dedir)" || $(MKDIR_P) "$(DESTDIR)$(man7dedir)"
	@list='$(man7de_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man7deDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man7dedir)/$$f'"; \
	  $(man7deDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man7dedir)/$$f"; \
	done

uninstall-man7deDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man7de_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man7dedir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man7dedir)/$$f"; \
	done
install-man7esDATA: $(man7es_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man7esdir)" || $(MKDIR_P) "$(DESTDIR)$(man7esdir)"
	@list='$(man7es_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man7esDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man7esdir)/$$f'"; \
	  $(man7esDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man7esdir)/$$f"; \
	done

uninstall-man7esDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man7es_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man7esdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man7esdir)/$$f"; \
	done
install-man7frDATA: $(man7fr_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man7frdir)" || $(MKDIR_P) "$(DESTDIR)$(man7frdir)"
	@list='$(man7fr_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man7frDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man7frdir)/$$f'"; \
	  $(man7frDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man7frdir)/$$f"; \
	done

uninstall-man7frDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man7fr_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man7frdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man7frdir)/$$f"; \
	done
install-man7ptDATA: $(man7pt_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man7ptdir)" || $(MKDIR_P) "$(DESTDIR)$(man7ptdir)"
	@list='$(man7pt_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man7ptDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man7ptdir)/$$f'"; \
	  $(man7ptDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man7ptdir)/$$f"; \
	done

uninstall-man7ptDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man7pt_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man7ptdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man7ptdir)/$$f"; \
	done
install-man7ruDATA: $(man7ru_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man7rudir)" || $(MKDIR_P) "$(DESTDIR)$(man7rudir)"
	@list='$(man7ru_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man7ruDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man7rudir)/$$f'"; \
	  $(man7ruDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man7rudir)/$$f"; \
	done

uninstall-man7ruDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man7ru_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man7rudir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man7rudir)/$$f"; \
	done
install-man7skDATA: $(man7sk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man7skdir)" || $(MKDIR_P) "$(DESTDIR)$(man7skdir)"
	@list='$(man7sk_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man7skDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man7skdir)/$$f'"; \
	  $(man7skDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man7skdir)/$$f"; \
	done

uninstall-man7skDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man7sk_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man7skdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man7skdir)/$$f"; \
	done
install-man7srDATA: $(man7sr_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man7srdir)" || $(MKDIR_P) "$(DESTDIR)$(man7srdir)"
	@list='$(man7sr_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man7srDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man7srdir)/$$f'"; \
	  $(man7srDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man7srdir)/$$f"; \
	done

uninstall-man7srDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man7sr_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man7srdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man7srdir)/$$f"; \
	done
install-man7ukDATA: $(man7uk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(man7ukdir)" || $(MKDIR_P) "$(DESTDIR)$(man7ukdir)"
	@list='$(man7uk_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(man7ukDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(man7ukdir)/$$f'"; \
	  $(man7ukDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(man7ukdir)/$$f"; \
	done

uninstall-man7ukDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(man7uk_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(man7ukdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(man7ukdir)/$$f"; \
	done
tags: TAGS
TAGS:

ctags: CTAGS
CTAGS:


distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -pR $(srcdir)/$$file $(distdir)$$dir || exit 1; \
	    fi; \
	    cp -pR $$d/$$file $(distdir)$$dir || exit 1; \
	  else \
	    test -f $(distdir)/$$file \
	    || cp -p $$d/$$file $(distdir)/$$file \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(MANS) $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(man1dir)" "$(DESTDIR)$(man5dir)" "$(DESTDIR)$(man7dir)" "$(DESTDIR)$(man1dedir)" "$(DESTDIR)$(man1esdir)" "$(DESTDIR)$(man1frdir)" "$(DESTDIR)$(man1itdir)" "$(DESTDIR)$(man1ptdir)" "$(DESTDIR)$(man1rudir)" "$(DESTDIR)$(man1skdir)" "$(DESTDIR)$(man1srdir)" "$(DESTDIR)$(man1ukdir)" "$(DESTDIR)$(man5dedir)" "$(DESTDIR)$(man5esdir)" "$(DESTDIR)$(man5frdir)" "$(DESTDIR)$(man5ptdir)" "$(DESTDIR)$(man5rudir)" "$(DESTDIR)$(man5skdir)" "$(DESTDIR)$(man5srdir)" "$(DESTDIR)$(man7dedir)" "$(DESTDIR)$(man7esdir)" "$(DESTDIR)$(man7frdir)" "$(DESTDIR)$(man7ptdir)" "$(DESTDIR)$(man7rudir)" "$(DESTDIR)$(man7skdir)" "$(DESTDIR)$(man7srdir)" "$(DESTDIR)$(man7ukdir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

info: info-am

info-am:

install-data-am: install-man install-man1deDATA install-man1esDATA \
	install-man1frDATA install-man1itDATA install-man1ptDATA \
	install-man1ruDATA install-man1skDATA install-man1srDATA \
	install-man1ukDATA install-man5deDATA install-man5esDATA \
	install-man5frDATA install-man5ptDATA install-man5ruDATA \
	install-man5skDATA install-man5srDATA install-man7deDATA \
	install-man7esDATA install-man7frDATA install-man7ptDATA \
	install-man7ruDATA install-man7skDATA install-man7srDATA \
	install-man7ukDATA

install-dvi: install-dvi-am

install-exec-am:

install-html: install-html-am

install-info: install-info-am

install-man: install-man1 install-man5 install-man7

install-pdf: install-pdf-am

install-ps: install-ps-am

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-man uninstall-man1deDATA uninstall-man1esDATA \
	uninstall-man1frDATA uninstall-man1itDATA uninstall-man1ptDATA \
	uninstall-man1ruDATA uninstall-man1skDATA uninstall-man1srDATA \
	uninstall-man1ukDATA uninstall-man5deDATA uninstall-man5esDATA \
	uninstall-man5frDATA uninstall-man5ptDATA uninstall-man5ruDATA \
	uninstall-man5skDATA uninstall-man5srDATA uninstall-man7deDATA \
	uninstall-man7esDATA uninstall-man7frDATA uninstall-man7ptDATA \
	uninstall-man7ruDATA uninstall-man7skDATA uninstall-man7srDATA \
	uninstall-man7ukDATA

uninstall-man: uninstall-man1 uninstall-man5 uninstall-man7

.MAKE: install-am install-strip

.PHONY: all all-am check check-am clean clean-generic distclean \
	distclean-generic distdir dvi dvi-am html html-am info info-am \
	install install-am install-data install-data-am install-dvi \
	install-dvi-am install-exec install-exec-am install-html \
	install-html-am install-info install-info-am install-man \
	install-man1 install-man1deDATA install-man1esDATA \
	install-man1frDATA install-man1itDATA install-man1ptDATA \
	install-man1ruDATA install-man1skDATA install-man1srDATA \
	install-man1ukDATA install-man5 install-man5deDATA \
	install-man5esDATA install-man5frDATA install-man5ptDATA \
	install-man5ruDATA install-man5skDATA install-man5srDATA \
	install-man7 install-man7deDATA install-man7esDATA \
	install-man7frDATA install-man7ptDATA install-man7ruDATA \
	install-man7skDATA install-man7srDATA install-man7ukDATA \
	install-pdf install-pdf-am install-ps install-ps-am \
	install-strip installcheck installcheck-am installdirs \
	maintainer-clean maintainer-clean-generic mostlyclean \
	mostlyclean-generic pdf pdf-am ps ps-am uninstall uninstall-am \
	uninstall-man uninstall-man1 uninstall-man1deDATA \
	uninstall-man1esDATA uninstall-man1frDATA uninstall-man1itDATA \
	uninstall-man1ptDATA uninstall-man1ruDATA uninstall-man1skDATA \
	uninstall-man1srDATA uninstall-man1ukDATA uninstall-man5 \
	uninstall-man5deDATA uninstall-man5esDATA uninstall-man5frDATA \
	uninstall-man5ptDATA uninstall-man5ruDATA uninstall-man5skDATA \
	uninstall-man5srDATA uninstall-man7 uninstall-man7deDATA \
	uninstall-man7esDATA uninstall-man7frDATA uninstall-man7ptDATA \
	uninstall-man7ruDATA uninstall-man7skDATA uninstall-man7srDATA \
	uninstall-man7ukDATA

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
